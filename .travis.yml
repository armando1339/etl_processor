sudo: false
language: ruby
rvm:
  - 2.6.3
services:
  - postgresql
before_install:
  - gem install bundler -v 1.17.3
before_script:
  - cd spec/etl_app
  - bundle exec rake db:create
  - bundle exec rake db:migrate
  - cd ../../